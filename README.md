# PackForge v3.9 — Auction & Polish

- Collector Auction is the only live event.
- Campaign animation rebuilt into a full visual card arena.
- Ultra cards use a flowing rainbow border and small colorful light bursts.
- Coin Flip lets the player choose Heads or Tails.
- Upgrades and Settings redesigned.
- All CSS is bundled into one deploy asset and all JavaScript into one deploy asset to minimize Vercel edge requests.
- Service-worker navigation is true cache-first and does not background-fetch index.html on every reopen.
- Local save key remains `packforge_save_v1`.
